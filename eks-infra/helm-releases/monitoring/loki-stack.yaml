releases:
  - name: loki-stack
    namespace: monitoring
    chart: grafana/loki-stack
    repo: https://grafana.github.io/helm-charts
    version: "2.10.0"
    values:
      - loki:
          persistence:
            enabled: true
            size: 50Gi
      - promtail:
          enabled: true
